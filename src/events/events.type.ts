export enum EventType {
  PROCESS_DETECT = 'process.detect',
  ROOM_JOIN = 'room.join',
  GROUP_CREATED = 'group.created',
  GROUP_UPDATED = 'group.updated',
  MESSAGE_CREATED = 'message.created',
  MESSAGE_TYPING = 'message.typing',
  TOLLAWARE_REFRESH = 'tollaware.refresh',
}
